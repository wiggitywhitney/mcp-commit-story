[pytest]
testpaths = tests
addopts = -ra
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests as integration tests (may be slow, require external dependencies)
    manual: marks tests that require manual setup or verification 